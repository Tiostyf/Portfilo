# render.yaml
services:
  - type: web
    name: rishabh-portfolio
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      NODE_VERSION: 20.17.0
      PORT: 10000
      MONGO_URI: 
        fromDatabase:
          name: portfolio-db
          property: connectionString
      EMAIL_USER: "rishabhsinghthakur999@gmail.com"
      EMAIL_PASS: 
        sync: false   # You will set this manually in Render Dashboard (App Password)

databases:
  - name: portfolio-db
    plan: free
    databaseName: portfolio
    user: rishabh